<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Helvetica Neue', Arial, sans-serif;
      font-size: 11px;
      color: #333;
      line-height: 1.5;
    }
    .container { padding: 10px; }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 20px;
      padding-bottom: 15px;
      border-bottom: 3px solid #7c3aed;
    }
    .company-info { flex: 1; }
    .company-logo {
      max-height: 60px;
      max-width: 160px;
      margin-bottom: 8px;
      display: block;
    }
    .company-name {
      font-size: 20px;
      font-weight: bold;
      color: #7c3aed;
      margin-bottom: 5px;
    }
    .company-details { font-size: 10px; color: #555; }
    .company-details p { margin-bottom: 2px; }
    .company-legal { font-size: 9px; color: #777; margin-top: 5px; }

    /* Document title */
    .doc-title { text-align: right; flex: 0 0 auto; }
    .doc-title h1 {
      font-size: 24px;
      color: #7c3aed;
      margin-bottom: 5px;
    }
    .doc-number {
      font-size: 14px;
      font-weight: bold;
      color: #333;
    }
    .doc-dates {
      margin-top: 8px;
      font-size: 10px;
      color: #555;
    }
    .doc-dates p { margin-bottom: 2px; }
    .statut-badge {
      display: inline-block;
      padding: 3px 10px;
      border-radius: 3px;
      font-size: 9px;
      font-weight: bold;
      margin-top: 6px;
      background-color: #ede9fe;
      color: #7c3aed;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    /* Client section */
    .client-section {
      margin-bottom: 20px;
      display: flex;
      justify-content: space-between;
      gap: 15px;
    }
    .client-box {
      border: 1px solid #ddd;
      border-radius: 4px;
      padding: 12px 15px;
      flex: 1;
      background-color: #f9fafb;
    }
    .client-box-title {
      font-size: 9px;
      text-transform: uppercase;
      color: #777;
      margin-bottom: 5px;
      letter-spacing: 0.5px;
    }
    .client-name {
      font-size: 13px;
      font-weight: bold;
      color: #333;
      margin-bottom: 3px;
    }
    .client-details { font-size: 10px; color: #555; }
    .client-details p { margin-bottom: 1px; }

    /* Table */
    .items-table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }
    .items-table thead th {
      background-color: #7c3aed;
      color: white;
      padding: 8px 10px;
      text-align: left;
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.3px;
    }
    .items-table thead th:first-child { border-radius: 4px 0 0 0; }
    .items-table thead th:last-child { border-radius: 0 4px 0 0; }
    .items-table tbody td {
      padding: 8px 10px;
      border-bottom: 1px solid #eee;
      font-size: 10px;
    }
    .items-table tbody tr:nth-child(even) { background-color: #f9fafb; }
    .items-table .text-right { text-align: right; }
    .items-table .text-center { text-align: center; }

    /* Totals */
    .totals-section {
      display: flex;
      justify-content: flex-end;
      margin-bottom: 15px;
    }
    .totals-table { width: 280px; border-collapse: collapse; }
    .totals-table td { padding: 5px 10px; font-size: 11px; }
    .totals-table .label { text-align: right; color: #555; }
    .totals-table .value { text-align: right; font-weight: 600; }
    .totals-table .total-row td {
      border-top: 2px solid #7c3aed;
      font-size: 14px;
      font-weight: bold;
      color: #7c3aed;
      padding-top: 8px;
    }
    .amount-words {
      font-style: italic;
      font-size: 10px;
      color: #555;
      margin-bottom: 20px;
      text-align: right;
    }

    /* Accord box */
    .accord-section {
      margin-top: 30px;
      display: flex;
      justify-content: space-between;
      gap: 20px;
    }
    .accord-box {
      flex: 1;
      border: 1px solid #ddd;
      border-radius: 4px;
      padding: 12px 15px;
      background-color: #faf5ff;
    }
    .accord-box-title {
      font-size: 10px;
      font-weight: bold;
      color: #7c3aed;
      margin-bottom: 40px;
    }
    .accord-line {
      border-top: 1px solid #ccc;
      padding-top: 5px;
      font-size: 9px;
      color: #555;
    }

    /* Conditions */
    .conditions {
      margin-top: 15px;
      padding: 12px;
      background-color: #f9fafb;
      border-radius: 4px;
      border: 1px solid #eee;
    }
    .conditions-title {
      font-size: 10px;
      font-weight: bold;
      color: #333;
      margin-bottom: 5px;
    }
    .conditions-text { font-size: 9px; color: #555; }

    /* Footer */
    .footer {
      margin-top: 25px;
      padding-top: 10px;
      border-top: 1px solid #ddd;
      text-align: center;
      font-size: 8px;
      color: #999;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="company-info">
        {{#if company.logo}}
        <img src="{{company.logo}}" alt="Logo" class="company-logo" />
        {{/if}}
        <div class="company-name">{{company.name}}</div>
        <div class="company-details">
          {{#if company.legalForm}}<p>{{company.legalForm}}</p>{{/if}}
          {{#if company.address.street}}<p>{{company.address.street}}</p>{{/if}}
          <p>{{company.address.city}}{{#if company.address.region}}, {{company.address.region}}{{/if}} - {{company.address.country}}</p>
          {{#if company.phone}}<p>Tel: {{company.phone}}</p>{{/if}}
          {{#if company.email}}<p>Email: {{company.email}}</p>{{/if}}
        </div>
        <div class="company-legal">
          {{#if company.ninea}}<p>NINEA: {{company.ninea}}</p>{{/if}}
          {{#if company.rccm}}<p>RC: {{company.rccm}}</p>{{/if}}
        </div>
      </div>
      <div class="doc-title">
        <h1>BON DE COMMANDE</h1>
        <div class="doc-number">N° {{commande.numero}}</div>
        <div class="doc-dates">
          <p>Date: {{formatDate commande.dateCommande}}</p>
          {{#if commande.dateLivraisonPrevue}}<p>Livraison prevue: {{formatDate commande.dateLivraisonPrevue}}</p>{{/if}}
        </div>
        <div class="statut-badge">{{commande.statut}}</div>
      </div>
    </div>

    <!-- Client -->
    <div class="client-section">
      <div class="client-box">
        <div class="client-box-title">Client</div>
        <div class="client-name">{{commande.clientSnapshot.displayName}}</div>
        <div class="client-details">
          {{#if commande.clientSnapshot.address.street}}<p>{{commande.clientSnapshot.address.street}}</p>{{/if}}
          {{#if commande.clientSnapshot.address.city}}<p>{{commande.clientSnapshot.address.city}}{{#if commande.clientSnapshot.address.region}}, {{commande.clientSnapshot.address.region}}{{/if}}</p>{{/if}}
          {{#if commande.clientSnapshot.phone}}<p>Tel: {{commande.clientSnapshot.phone}}</p>{{/if}}
          {{#if commande.clientSnapshot.email}}<p>{{commande.clientSnapshot.email}}</p>{{/if}}
          {{#if commande.clientSnapshot.ninea}}<p>NINEA: {{commande.clientSnapshot.ninea}}</p>{{/if}}
        </div>
      </div>
      {{#if commande.devis}}
      <div class="client-box">
        <div class="client-box-title">Reference devis</div>
        <div class="client-name">{{commande.devis.numero}}</div>
      </div>
      {{/if}}
    </div>

    <!-- Items table -->
    <table class="items-table">
      <thead>
        <tr>
          <th style="width: 30px;">N°</th>
          <th>Designation</th>
          <th style="width: 50px;" class="text-center">Ref</th>
          <th style="width: 50px;" class="text-center">Qte</th>
          <th style="width: 50px;" class="text-center">Livree</th>
          <th style="width: 45px;" class="text-center">Unite</th>
          <th style="width: 80px;" class="text-right">PU HT</th>
          <th style="width: 45px;" class="text-center">Rem%</th>
          <th style="width: 40px;" class="text-center">TVA%</th>
          <th style="width: 90px;" class="text-right">Total HT</th>
        </tr>
      </thead>
      <tbody>
        {{#each commande.lignes}}
        <tr>
          <td class="text-center">{{inc @index}}</td>
          <td>{{this.designation}}</td>
          <td class="text-center">{{this.reference}}</td>
          <td class="text-center">{{this.quantite}}</td>
          <td class="text-center">{{#if this.quantiteLivree}}{{this.quantiteLivree}}{{else}}-{{/if}}</td>
          <td class="text-center">{{this.unite}}</td>
          <td class="text-right">{{formatMontant this.prixUnitaire}}</td>
          <td class="text-center">{{#if this.remise}}{{this.remise}}%{{else}}-{{/if}}</td>
          <td class="text-center">{{this.tauxTVA}}%</td>
          <td class="text-right">{{formatMontant this.montantHT}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>

    <!-- Totals -->
    <div class="totals-section">
      <table class="totals-table">
        <tr>
          <td class="label">Total HT :</td>
          <td class="value">{{formatMontant commande.totalHT}} FCFA</td>
        </tr>
        {{#if commande.remiseGlobale}}
        <tr>
          <td class="label">Remise ({{commande.remiseGlobale}}%) :</td>
          <td class="value">-{{formatMontant (multiply commande.totalHT (multiply commande.remiseGlobale 0.01))}} FCFA</td>
        </tr>
        {{/if}}
        <tr>
          <td class="label">TVA (18%) :</td>
          <td class="value">{{formatMontant commande.totalTVA}} FCFA</td>
        </tr>
        <tr class="total-row">
          <td class="label">Total TTC :</td>
          <td class="value">{{formatMontant commande.totalTTC}} FCFA</td>
        </tr>
      </table>
    </div>

    <div class="amount-words">
      Arrete la presente commande a la somme de : <strong>{{numberToWords commande.totalTTC}}</strong>
    </div>

    <!-- Conditions -->
    {{#if commande.conditionsPaiement}}
    <div class="conditions">
      <div class="conditions-title">Conditions de paiement</div>
      <div class="conditions-text">{{commande.conditionsPaiement}}</div>
    </div>
    {{/if}}

    {{#if commande.notes}}
    <div class="conditions" style="margin-top: 10px;">
      <div class="conditions-title">Notes</div>
      <div class="conditions-text">{{commande.notes}}</div>
    </div>
    {{/if}}

    <!-- Bon pour accord -->
    <div class="accord-section">
      <div class="accord-box">
        <div class="accord-box-title">Bon pour accord — Le client</div>
        <div class="accord-line">Date et signature</div>
      </div>
      <div class="accord-box">
        <div class="accord-box-title">Etabli par — {{company.name}}</div>
        <div class="accord-line">Cachet et signature</div>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>{{company.name}} - {{company.address.street}}, {{company.address.city}}, {{company.address.country}}</p>
      {{#if company.ninea}}<p>NINEA: {{company.ninea}} {{#if company.rccm}}- RC: {{company.rccm}}{{/if}}</p>{{/if}}
      <p>Document genere le {{formatDate generatedAt}}</p>
    </div>
  </div>
</body>
</html>
